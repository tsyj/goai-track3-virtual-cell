# 复赛迭代台账（2026-09-02 起）

> 规则：过线 = 六无孤儿折逐折配对 delta > 2×sem，真配置 booster 复核后才进提交；
> 模型冻结线 9/3 08:00。每轮记录：假设 → 实验 → 判决（含否决）。

| # | 假设 | 实验 | 判决 | 出处 |
|---|---|---|---|---|
| 0a | 基因组可预测未见菌株基线 b | 63/64/65：PAV/移码/CNV → b 回归与打分 | ❌ 机制真（−0.34/−0.41）但覆盖 2%，+0.0003；**不采纳**，进报告当定量负结果 | genome_*.csv |
| 0b | 8/27 两个候选成员（pert×32、320 成分）可进集成 | 缓存重评 ×2 | ❌ +0.00024（=2×sem，5/6）与 −0.00009；**维持 12 成员** | pool_real_eval.csv |
| 1a | F 族加权 >0.5 更优 | 缓存细扫 w∈[0.5,0.8] | ❌ 全部 3/6，不过线；**等权维持** | 本轮日志 |
| 1b | booster 侧结构消融可产出新成员 | 便宜筛 + 缓存贡献评估 | ❌ 三个成员对集成 −0.0001~−0.0003（0/6）；加性层的结构轴有效、booster 层无效 | 56 eval |
| 1c | 未见菌株行的 booster 输出需要重新定标 | 6 折扫 k∈[0.6,3.0]，便宜+真配置双口径 | ✅ **中**：峰值 k=1.8 (+0.00139±0.00010, 6/6, 14×sem)，两口径曲线重合；机制=树对未见类别水平输出塌向分裂默认侧；终验见 3a | unseen_booster_scale_*.csv |
| 2a | 同机制迁移到未见化合物行 | 缓存部件扫 (k_s,k_c) 网格 | ❌ k_chem>1 单调变差（chem 行已贴噪声地板，放大的是噪声）；k_chem=1 | unseen_chem_scale_cheap.csv |
| 2b | F 族加权、booster 消融见 1a/1b | — | 均否决 | — |

| 3a | 1c 的效应在部署口径下是否成立 | val 镜像 12 成员 + 单成员 A，k∈[1.0,2.0] | ❌ **不采纳**：val（模型见 4 菌株）曲线平坦（峰 +0.0002@k=1.2-1.4，k≥1.6 转负），单成员同样平坦 ⇒ 效应量随可见菌株数衰减，内层折（见 3 菌株）的 14×sem 不迁移到部署口径。**同构性原则第二例**（孤儿 plate 之后）。k=1.0，提交不变 | val_scale_eval.csv |

| 4 | 训练期修复塌缩：strain 特征 dropout 增强 | 便宜 6 折，p∈{0.15,0.3,0.5} | ❌ 全负（−0.0004~−0.0008），FC[strain_only] 也变差——增强稀释信号 | strain_dropout.csv |

| 5 | 孔位（板边缘）效应进加性层 | 6 折，row/col、row×inst、edge | ❌ **−0.03**：孔位与化合物完全混淆（pert_id=槽位号），孔位项吸走化合物响应，未见化合物行被塞进该孔上次的药，M3 −0.09 | well_factors.csv |
| 6 | booster SVD 前按蛋白标准化 | 6 折 sqrt/std | ❌ +0.0001±0.0003，M3 反降 | std_svd_booster.csv |
| 7 | **大效应非线性扩张** h(D)=D(1+β·min(|D|/τ,1)²) | 12 成员真配置缓存，6 折 + val 镜像 | ✅ **采纳**：内层 +0.0005~0.0007 全平台 6/6（3–5×sem）；**val 镜像 0.5032→0.5039**；采纳 β=0.3 τ=1.25。机制：收缩把大效应压小，M2/M4/M6 尾部还回来。需产物存全部行成分得分 → 重训 12 成员 | expand_inner_fine.csv, expand_val.csv |
| 8 | booster 成分按序号衰减 | 6 折 6 种日程 | ❌ 最好 +0.00004 | comp_decay.csv |
| 9 | 三阶交互 / 化合物×时间段 | 6 折 4 变体 + 成员候选 | ❌ strain×med×temp 单看 +0.00033(5/6) 不过线；做成员 −0.00012(0/6)；其余 ≤0 | higher_order.csv |
| 11 | 尾部扩张的精修变体 | 12 成员缓存 6 折 + val | 逐成员先扩=等价；逐蛋白 τ +0.0001；单向扩张负；**只扩可见菌株行 −0.0011/val −0.0009；只扩零标签菌株行 +0.0004/val +0.0003** | expand_variants_inner.csv |
| 12 | **零标签菌株行的尾部扩张重标定** | (β,τ) 网格，内层 + val | ✅ **采纳 v2.2**：β=0.7 τ=0.75，内层 +0.0020±0.0002（6/6，9.8×sem），val 0.5032→**0.5050**；平台宽（β.5–.8, τ.5–1）。与 #3a 同机制、跨口径成立 | unseen_expand_grid2_*.csv |
| 13 | 参照方向载荷校正（把 D̂ 在 μ_ctx/μ_drug 上的投影系数拉向 1） | 12 成员缓存 6 折 + val | ❌ 灾难性：ctx −0.04/−0.08，drug −0.0014/−0.0037 | ref_loading_regime_*.csv |
| 14 | 按体制（可见/未见菌株×化合物）分别定 F 族权重 | 缓存 6 折 + val | ❌ 未见菌株行 w=0.7：+0.0002（3/6），val +0.0004；不过线 | ref_loading_regime_*.csv |
| 16 | 扩张锚点 C 对对照约定（DMSO/Water/全部/三者平均）的敏感性 | 缓存 6 折 + val | ✅ 稳健：全部对照（现行）≈三者平均；DMSO-only −0.0003/val −0.0005；Water-only ±0。维持全部对照孔均值 | anchor_convention_inner.csv |
| 15 | 公开实测蛋白质组（PNAS 2024，942 株，同批分离株）直接读出未见菌株基线 b | 留一 κ 标定 + 缓存 6 折 + val（87） | ❌ 机制真但弱：外部对比与 b 相关 0.30–0.36（CEK 0.11），κ≈0.1，覆盖 13–18% 蛋白；内层 +0.0003（6/6）、val +0.0001 → 不采纳；**同批菌株另一平台的实测蛋白质组也只解释 b 的 ~10%** | extprot_corr_*.csv, extprot_diag.csv |
| 17 | 集成聚合改为按样本方向平均 | 缓存 6 折 + val | ❌ ±0.0001 | direction_avg_inner.csv |
| 18 | 未见化合物的专家机制类供体迁移（HU←Cisplatin、氟康唑←Clotrimazole、两性霉素←Nystatin…） | val 6 化合物（4 有供体）+ 内层 | ❌ val k=0.25 +0.0003、k≥0.5 转负；HU←Cisplatin 的 Δ-PCC 0.474→0.470；内层 +0.0003(4/6) → 与化学相似度迁移同结论：**未见化合物特异响应不可外推，专家机制映射也不行** | moa_donor_inner.csv |
| 19 | 时间插值行：同条件两侧时间点训练残差线性插值叠加 | 缓存 val(157 行, 103 有邻居) + 内层 | ❌ 单调负（α=0.5 内层 −0.0004，val −0.0005） | time_interp_inner.csv |
| 20 | 未见菌株先验中心改为菌株均衡均值（擂台提案） | 数据核查 | ❌ 无需实验：训练行按菌株 1465–1492 近乎均衡，均衡均值≡行加权均值 | metadata |
| 21 | booster 残差基改为掩码感知（迭代低秩填补代替 NaN→0） | 便宜 6 折 | ❌ 不过线：iters=3 +0.0004±0.0003（5/6，1.4×sem），M4 +0.003 但 M3 −0.002 对冲；iters=6 ≈0 | masked_svd.csv |
| 22 | 未见菌株行路由到无 strain 特征booster | 便宜池缓存 6 折 | ❌ 加扩张后 −0.0009（0/6） | route_nostrain_cheap.csv |

**第四轮（09-03 凌晨）**：查清官方原文确认验证标签不可入训练；补写 README 4.2 声明；
发现并采纳「零标签菌株行参照放大」，val 0.5050→0.5079。

**旧收官判断（22:30，第三轮）**：擂台 18 条提案零幸存；本轮自测 12 条轴（#11–#22）中只有 #12（零标签菌株行尾部扩张重标定）采纳为 v2.2。模型冻结。

| 23 | 向冻结参照收缩（λ<1）与蛋白轴 γ<1 校准 | 缓存 6 折 + val | ❌ 全线大幅变差（chem λ=0.7 内层 −0.0136；γ=0.95 −0.0054）——模型已明显优于参照 | ref_blend_*.csv |
| 24 | 未见化合物行放大偏离 μ_ctx 的分量 | 6 折 + val | ❌ 单调变差（λ=1.5 内层 −0.0120，M3 −0.017）：那些行的偏离基本是噪声 | amplify_dev_*.csv |
| 25 | **零标签菌株行放大偏离 μ_drug 的分量** | 6 折 + val | ✅ **采纳**：λ=1.55 内层 +0.0021（6/6，13×sem），val +0.0020，M4 +0.016 | amplify_dev_*.csv |
| 26 | 与尾部扩张联合调优 (λ, β) | 20 点网格 × 6 折 + val | ✅ **采纳 v2.3**：两者部分冗余，重新配比取平台中心 λ=1.55 β=0.40 τ=0.75；内层 +0.0023（6/6，7.5×sem），val **0.5032→0.5079**（交付代码复核） | joint_grid2_*.csv |
| 27 | 扩张机制的辩护性诊断 | val 镜像回归斜率 | ⚠️ **推翻原解释**：零标签菌株行斜率 0.758<1，按均方误差反该缩小 ⇒ 提分不是"收缩偏差修正"，而是 Δ 两边共享实测对照噪声导致的配比最优。报告按此诚实口径写 | 96_dispersion |
| 10 | 全局幂律 sign(D)|D|^γ 代替/叠加尾部扩张 | 12 成员缓存 6 折 | ❌ γ≥1.1 全负、γ=1.05 仅 3/6；**只动尾部的 h(D) 是唯一稳的形式** | power_shape.csv |

**收官判断（16:30 更新）**：第二轮九条轴再中一条（#7），已进入重训。

**旧收官判断（06:50，已被 #7 推翻）**：六条新轴（F 加权 / booster 消融 / 菌株重定标 / 化合物重定标 / 同构终验 / dropout 增强）全部系统性否决。
12 成员集成 = 当前信息条件下的天花板；提交 sha 保持 c6750c97 不变。模型冻结，火力转向报告/PPT/提交件。

## 已封死的轴（不再回头）
λ 邻域与家族缩放、n_pass、fit_offset、booster 容量/正则/种子数、交叉拟合 booster、
低秩截断（plate/化合物）、扰动放大、按蛋白/按样本校准、化学相似度迁移、基因组供体迁移、
基因组基线修正、embedding MLP、F 族加权。

## 排队中的候选（按先验）
（队列清空——收官）
