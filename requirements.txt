# Python 3.9.23 (conda env "numpy1"); CPU only
numpy==2.0.2
pandas==2.3.1
scipy==1.13.1
lightgbm==4.6.0
pyarrow==21.0.0
matplotlib==3.9.4      # figures only
rdkit==2025.9.2        # only vcell/chem.py (rejected chemistry-transfer experiments); not needed for the final model
pytest                 # tests/
